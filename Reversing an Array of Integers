#include<stdio.h>
void main()
{
int i,n,a[20];
printf("Enter the number of elements:");
scanf("%d",&n);
printf("Enter the array elements:");
for(i=0;i<n;i++)
{
  scanf("%d",&a[i]);
} 
i=0;
while(i<n)
{
  printf("%d",a[n-i-1]);
  i++;
}
}
